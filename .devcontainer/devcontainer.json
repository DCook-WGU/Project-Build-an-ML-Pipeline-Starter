{
  "name": "mlops-dev-project",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": "..",
    "args": {
      "ENV_NAME": "mlops-dev-project",                  
      "ENV_FILE": "/app/environment.yml"
    }
  },
  "workspaceFolder": "/app",
  "runArgs": [
    "-v", "${localWorkspaceFolder}:/app",
    "-e", "WANDB_API_KEY=${localEnv:WANDB_API_KEY}"
  ],
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash",
    "python.defaultInterpreterPath": "/opt/miniconda/envs/mlops-dev-project/bin/python"
  },
  "postCreateCommand": "git config --global user.name 'DCook-WGU' && git config --global user.email 'dcoo230@wgu.edu'",
  "remoteUser": "root",
  "continerName": "mlops-project",
  "extensions": [
    "ms-python.python",
    "ms-toolsai.jupyter",
    "ms-vscode-remote.remote-containers"
  ]
}
